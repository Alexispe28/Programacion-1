@model List<Factura>

<h1>Lista Facturas</h1>

<table class="table">
    <tr>
        <th>Código</th>
        <th>Cliente</th>
        <th>Fecha Realizada</th>
        <th>Total</th>
        <th>Opciones</th>
    </tr>

    @foreach(var p in Model) {
        <tr>
            <td>@p.Id_Factura</td>
            <td>@p.Cliente.Nombre</td>
            <td>@p.Fecha_Realizada</td>
            <td>@p.Total</td>
            <td>
                <a href="~/inventario/detalles/@p.Id_Factura" 
                class="btn btn-primary a-btn-slide-text"><span class="glyphicon glyphicon-edit"></a> | 
                <a href="~/inventario/eliminar/@p.Id_Factura" 
                class="btn btn-danger a-btn-slide-text"><span class="glyphicon glyphicon-remove"></a>
            </td>
        </tr>
    }
    

</table>
<a href="~/categoria/registrar" 
class="btn btn-primary a-btn-slide-text">Nueva Categoria</a>